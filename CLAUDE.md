# Pariyesana

## Deployment

- Production server: `ssh oci-pariyesana`
- Project lives at `~/pariyesana` on the remote
- Deploy: `ssh oci-pariyesana "cd pariyesana && git pull && docker compose up -d --build"`
